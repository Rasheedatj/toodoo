import { StyleSheet } from 'react-native';

export const tabParentStyles = StyleSheet.create({
  container: {
    flex: 1,
    // justifyContent: 'center',
    padding: 16,
    paddingTop: 70,
    // alignItems: 'center',
  },
});
